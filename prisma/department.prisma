model Department {
  id        String    @id @default(uuid())
  name      String    @unique
  code      String    @unique
  courses   Course[]
  faculty   Faculty[]
  students  Student[]
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  @@map("departments")
}
